// Telegram bot username for the Telegram Login Widget.
// This is NOT secret — it's rendered in the DOM as data-telegram-login in login-page.component.ts.
// Committed as single environment file for all contexts (dev, production, Docker, CI).
export const environment = {
  telegramBotUsername: 'shopo220v_bot',
};

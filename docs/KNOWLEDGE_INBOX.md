# Knowledge Inbox

Append-only queue for durable, project-relevant learnings whose final home isn't clear yet. Distilled into PROJECT_CONTEXT.md / CLAUDE.md / a rule / a skill, then deleted from here — this file should trend toward empty.

## 2026-07-07 — cts-sync.sh: self-overwrite of .claude/scripts/cts-sync.sh mid-run produces a spurious syntax-error exit code

Why: `cts-sync.sh` copies `.claude/scripts/` last in the payload order specifically so it overwrites itself only after everything else is synced (see comment above `sync_path()`). But bash does not fully buffer a script's source before executing straight-through code — when `copy_one` overwrites the running script file on disk, bash's subsequent reads for the remainder of the script land on bytes from the _new_ file at the _old_ file's byte offset, which don't align to any coherent statement. Observed result: the run completes all real work (payload copied/merged, `.cts-version` written, "Done. Review with: git diff" printed) but then bash throws a parse error and the process exits non-zero. Verified via `.cts-version` matching the new upstream SHA and `git diff --stat` matching the printed merge/conflict list — the non-zero exit does not mean the sync failed, contradicting the assumption in `.claude/skills/cts-update/SKILL.md` step 2 that non-zero exit means "did not run." Re-verified 2026-07-07 in an isolated sandbox after this exact sync pulled a new `cts-sync.sh` revision (commit `c512bd8`, which only reworked `merge_one`'s temp-file cleanup and left the self-copy ordering/comment untouched): reproduced the same class of failure with a synthetic mid-file upstream diff to `cts-sync.sh` (exit 127, `line: command not found` — a _different_ garbled error than the original run's `exit 2, syntax error near unexpected token '&&'`, confirming the error text/code is arbitrary noise from misaligned byte offsets, not a stable signal) — so the bug is still present in the version currently installed at `.claude/scripts/cts-sync.sh`. Root cause is structural (self-modifying script under `bash script.sh` invocation) and needs a fix in the sync engine itself, e.g. re-exec via `exec bash "$0" "$@"` right before the self-copy, or defer the self-copy to a `trap ... EXIT` that runs after the interpreter has already finished reading the file, or simply copy `.claude/scripts/cts-sync.sh` to a temp path and `cp` it into place as the very last statement with no code after it. Belongs in: .claude/scripts/cts-sync.sh (code fix — approach not yet decided, see three candidates above) — also `.claude/skills/cts-update/SKILL.md` step 2 wording should note this known false-negative on exit code

## 2026-07-13 — workspace (scoped-admin) feature: fully planned but deferred in full, consolidated to one doc

Why: Full `ba` → `ddd-architect` → `devil` planning session (2026-07-12) resolved the entity name (`Workspace`, not tenant/group/organization), aggregate design (membership-in-aggregate, hard ≥1-admin invariant), bounded-context placement (new `libs/workspace/*` scope, not folded into `identity`), and the claim-shape/staleness tradeoff (live DB re-check for scoped-admin mutations, JWT `scopedRoles` claim advisory-only). A follow-up `grill-me` session (2026-07-13) then resolved the remaining product questions (workspace creation is CLI-only/superadmin-operated; membership is admin-invited-only with no pending/approval state; workspace-admin actions are web-UI-only and not scheduled) — and the owner decided to defer the **entire** feature (not just parts) since there is currently no near-term caller for any of it. The whole design now lives in one consolidated file rather than the original 5-file split, since fragmenting a fully-parked feature added no value: `tasks/workspace/parked/2026-07-13-01-workspace-feature-consolidated-context.md`. Also confirmed as a reusable domain fact: today's flat single-role model is already behaviorally equivalent to "one implicit workspace, superadmin as its admin" — deferring the feature is not a functional gap. Also surfaced: `SessionGuard` (`apps/api/src/auth/session.guard.ts`) already re-fetches `User.status` live per-request while trusting stale `roles` from the JWT — the existing precedent the parked live-check design would extend, not a new pattern. Belongs in: PROJECT_CONTEXT.md (once `libs/workspace/*` actually lands — this entry is a pointer so the reasoning isn't lost between now and implementation, not a substitute for that doc)

## 2026-07-15 — [IMPORTANT — contribute to CTS] docs: markdown prose wrapping is now enforced by Prettier (`proseWrap: never`), not a hand-written agent rule

Why: A first attempt fixed hard-wrapped prose in `docs/SKELETON.md` and added `rules/docs-style.md` as a manual convention for agents to follow — but manual/agent reflow missed several list-item continuations (README numbered steps, DECISIONS "alternatives rejected" bullets) that a mechanical tool catches every time. Since `.md` files already run through `prettier --write` on every commit via `lint-staged`, added `"proseWrap": "never"` to `.prettierrc` instead and ran it across all project-owned docs (root docs, `docs/**`, `rules/**`, `libs/**/README.md`; `.claude/**` and vendored `postgres-best-practices` skill excluded from this pass). Discovered and fixed two pre-existing landmines this exposed: Prettier's markdown parser merges any block with no blank line between two logically-distinct lines into one paragraph — this silently destroyed a table in `rules/workflow.md` ("Frontend agent selection" table had no blank line before it) and glued together two bold-label lines in `libs/identity/infrastructure/README.md` (`**Tags**`/`**May import**` with no blank line between them). Both were pre-existing markdown-authoring bugs, not something Prettier introduced — fixed by adding the missing blank line at the source. `rules/docs-style.md` was rewritten to describe Prettier as the enforcement mechanism (plus the blank-line-before-structural-element gotcha) rather than a convention agents must remember. User flagged this as important and worth contributing upstream — logged in `docs/CLAUDE_TS_CHANGELOG.md` as `pending-port` (not yet run through `/cts-contribute`, deliberately deferred to a dedicated session since the inbox has 20 other unplaced entries that would otherwise gate that flow). When ported: CTS should receive both the `.prettierrc` setting/`rules/docs-style.md` file **and** a pass of `prettier --write` over CTS's own `.md` files, so the template repo's docs match the convention it's prescribing. Belongs in (guess): already landed locally as `.prettierrc` + `rules/docs-style.md`; still pending the actual `/cts-contribute` run — delete this entry once that's done

## 2026-07-21 — [web/config] `TELEGRAM_BOT_USERNAME` traversed five infrastructure hops to reach exactly one consumer; the API never read it at all

Why: A day of failing CI produced five successive fix commits (`53f0570`, `7571396`, `fe9aadf`, `ceb4dfb`, `b6e1a6b`, all reverted by `ca235cf`), each patching a different hop of the same chain: `.env` → `docker-compose.yml` build arg → Dockerfile `ARG` → `printf` interpolation into generated TypeScript → gitignored `apps/web/src/environments/environment.ts` → Angular bundle. A repo-wide search established that `apps/api` reads only `TELEGRAM_BOT_TOKEN` (`apps/api/src/config/api-config.ts:25`) and that no backend or CLI code reads the _username_ anywhere — so `.env`'s `TELEGRAM_BOT_USERNAME` existed solely to feed the web image's build arg. It was frontend config wearing backend clothes. Two compounding mistakes: the value was treated as a GitHub _secret_ despite being rendered into the DOM by the Telegram Login Widget (`login-page.component.ts:66` sets `data-telegram-login`), and the file it produced was gitignored, so every execution context (ci job, e2e job, Dockerfile, fresh clone) had to independently regenerate it before any `nx` target touching `web` could compile — there was no single place to be correct. The fix direction is deletion, not repair: commit `environment.ts` and remove the whole chain. Worth preserving separately: the Angular side is already well-factored — `TELEGRAM_BOT_USERNAME` is an `InjectionToken` provided once in `apps/web/src/app/app.config.ts:18`, so migrating later to a runtime `/api/config` endpoint (one image for all environments) is a one-line change at the provider, not a refactor. Belongs in (guess): PROJECT_CONTEXT.md (the config-ownership fact + the deferred runtime-config option); the generic lesson about gitignored generated files belongs in a rule

## 2026-07-21 — [ci] An unset GitHub Actions `secrets.*` evaluates to empty string with no error, and masking makes the resulting misconfiguration undebuggable — use `vars.*` for non-sensitive config

Why: The original 2026-07-18 `build-images` failure was `${{ secrets.TELEGRAM_BOT_USERNAME }}` resolving to nothing because the secret had not been created yet; the workflow passed an empty `--build-arg` and the Dockerfile's `test -n` guard emitted a generic message that pointed at the Dockerfile rather than at the missing secret. GitHub documents both halves of this trap: an unset secret silently becomes an empty string, and secrets cannot be used in `if:` conditionals, so you cannot cheaply assert their presence. Compounding it, secrets are masked as `***` in logs by design — which means when the value is merely _wrong_ rather than missing, the logs cannot distinguish the two. GitHub's own criterion is sensitivity, not repo-hygiene: "if you need greater security for sensitive information such as passwords or API keys, you should use secrets instead of variables" (docs.github.com, Variables › Security Considerations). Configuration **variables** (`vars.*`, scopable to repository/organization/environment) are the correct vehicle for non-sensitive per-environment values — they render in plain text, so they are debuggable. Rule of thumb established here: masking is a cost paid for confidentiality; when the value is not confidential you pay the cost and receive nothing. Belongs in (guess): rules/ (a CI/secrets-handling rule) — likely `claude-ts-upstream` too, since it is stack-agnostic

## 2026-07-21 — [nx] `nx affected` has no `-p`/`--projects` flag and silently forwards unrecognised flags to the underlying task command

Why: `pnpm nx affected -p web-e2e -t e2e` did not error at the nx level — nx passed `-p web-e2e` through to the task, producing `playwright test -p web-e2e` → `error: unknown option '-p'`, an error message that points at Playwright and gives no hint that the mistake was in the nx invocation. `--projects` exists on `nx run-many`, not on `nx affected`, and the two are easy to conflate. The replacement used at the time (`nx affected -t e2e --exclude=smoke-e2e,api-e2e`) works but is a denylist and will silently fail to exclude the fourth e2e project someone adds later; a positively-stated scoping mechanism (tags, a distinct target name, or simply `nx run web-e2e:e2e` when the job only ever runs one project) is preferable. Belongs in (guess): rules/workflow.md § Command Execution Policy (Nx Targets)

## 2026-07-21 — [ci] A GitHub Actions run marked "failure" can be mostly green — always read job-level conclusions before concluding the pipeline is broken

Why: The run for `b6e1a6b` showed as `failure` in the UI and in pasted logs, and was reasoned about (including by a dispatched `devops` agent working only from those logs) as "the pipeline is still failing." `gh run view <id> --json jobs` showed `ci` ✅, `build-images` ✅, `e2e` ❌ — two of three jobs were already green and the only remaining defect (missing Playwright firefox/webkit binaries) was unrelated to the environment-file work everyone was chasing. Run-level status is the OR of its jobs, so a 2/3-green run and a 0/3-red run are visually identical; pasted log excerpts flatten this further by showing one job at one moment on one branch. Practical consequence: an agent without GitHub API access cannot distinguish these states and will over-report breakage — authenticate `gh` (read scopes suffice: run status, job breakdown, `--log-failed`, and file contents at any ref were all that this investigation needed) before diagnosing CI. Belongs in (guess): rules/ (a CI-diagnosis rule) — plus a note that `gh` read access is worth having configured

## 2026-07-21 — [tooling] `.mcp.json` `${VAR}` placeholders fail open: an unset variable is passed through literally and surfaces as a vendor auth error, not a config error

Why: The project's committed `.mcp.json` declares `context7` as `npx -y @upstash/context7-mcp --api-key ${CONTEXT7_API_KEY}` (correctly avoiding a literal credential in a tracked file), but `CONTEXT7_API_KEY` was set nowhere — not in the environment, not in shell profiles, not in `.env`. Claude Code started the server anyway with the placeholder unexpanded, so every call returned Context7's _"Invalid API key. API keys should start with 'ctx7sk'"_ rather than anything indicating a missing variable. `claude mcp list` is the diagnostic — its footer reports both `Missing environment variables:` per server and `[Conflicting scopes]` collisions. Two further findings: `context7` was simultaneously defined at user scope (hosted `https://mcp.context7.com/mcp`) and via a plugin, giving three overlapping servers and duplicate toolsets (`mcp__context7__*` alongside `mcp__plugin_context7_context7__*`); and setting the variable under `env` in the gitignored `.claude/settings.local.json` took effect **without restarting Claude Code**, so a session's context need not be sacrificed to fix MCP credentials. The same unset-variable problem silently disabled the `figma` and `github` servers. Belongs in (guess): README.md § setup (the `.mcp.json` variables a contributor must supply) — the fail-open behaviour itself is `claude-ts-upstream` material

## 2026-07-21 — [ci] An `actions/cache` key must encode everything that determines the cached CONTENT, not just the lockfile — an exact key hit skips the save step, so a stale cache persists silently forever

Why: The Playwright browser cache was keyed `playwright-${{ hashFiles('pnpm-lock.yaml') }}`, but its content is determined by the installed browser SET, which the key did not encode. When the install step changed from `playwright install --with-deps chromium` to all three engines, `pnpm-lock.yaml` was untouched, so the key still hit exactly — and `actions/cache` skips its post-job save on an exact hit. The steady state would have been: restore chromium-only cache → download firefox + webkit → save nothing → repeat forever, with tests passing the whole time and nothing in the logs announcing the waste. Confirmed empirically in both directions: the pre-fix run's final step read `skipped Post Cache Playwright browsers`, while the post-fix run (key prefix bumped `playwright-` → `playwright-all-`, `restore-keys` bumped to match so a stale entry cannot be partially restored) read `Cache not found for input keys: playwright-all-…` → downloads → `Cache saved with key: playwright-all-…`. Generalises beyond Playwright to any cache whose content depends on a step's arguments rather than only on a manifest — the lockfile pins the tool VERSION, not which artefacts you asked it to fetch. Also recorded for calibration: the full three-engine e2e job runs 1m43s cold (18 tests, 21.4s of actual testing), so the "install all browsers is too slow for CI" premise that motivated the original chromium-only install did not hold. Belongs in (guess): rules/ (a CI-caching rule) — stack-agnostic, so `claude-ts-upstream` material too

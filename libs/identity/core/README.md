# identity-core

**Tags:** `scope:identity` · `type:core` · `platform:server`

`User` entity, `IUserRepository` interface, `UserStatus` enum (`pending` / `active` / `rejected`). Framework-free — no `@nestjs/*`, `mongoose`, or `@typegoose/*` imports. May import `scope:identity` + `scope:shared` kernel/contracts/errors/util.

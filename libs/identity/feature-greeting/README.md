# identity-feature-greeting

**Tags:** `scope:identity` · `type:feature` · `platform:web`

Angular greeting page for `active` users: calls `GET /api/hello` and renders a personalized greeting. May import `scope:identity` ui/data-access and `scope:shared` contracts/util. No server-only imports.

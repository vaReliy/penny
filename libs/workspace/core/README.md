# workspace-core

**Tags:** `scope:workspace` · `type:core` · `platform:server`

`Workspace` aggregate, `Membership` value object, `WorkspaceMemberRole`, and `IWorkspaceRepository`. Framework-free — no `@nestjs/*`, `mongoose`, or `@typegoose/*` imports. May import `scope:workspace` + `scope:shared` kernel/errors/util only — never `scope:identity` or `scope:budget`.

# shared-infrastructure

**Tags:** `scope:shared` · `type:infrastructure` · `platform:server`

Cross-domain server infrastructure: MongoDB connection factory (`createMongoConnection`, `disconnectMongoConnection`, `pingMongo`). May import `scope:shared` libs of any type. Never imported by `platform:web` code.

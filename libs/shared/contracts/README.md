# shared-contracts

**Tags:** `scope:shared` · `type:contracts` · `platform:shared`

Shared DTOs and request/response types consumed by both `apps/web` (Angular) and `apps/api` (NestJS). May import only `scope:shared` + `type:util`. No framework imports; no runtime logic.

# shared-kernel

**Tags:** `scope:shared` · `type:kernel` · `platform:shared`

Base onion abstractions: `BaseService` (LIVR-bootstrapped validate → execute contract), base repository interface, and `ServiceContext`. May import only `scope:shared` + `type:errors` + `type:util`.

# shared-util

**Tags:** `scope:shared` · `type:util` · `platform:shared`

Pure, framework-free helpers (e.g., `Money` value object, type guards, date utilities). May import only `scope:shared` + `type:util`. Leaf node — nothing in this lib imports other internal libs.

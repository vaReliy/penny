# shared-errors

**Tags:** `scope:shared` · `type:errors` · `platform:shared`

`BaseError` hierarchy (e.g., `ValidationError`, `NotFoundError`, `UnauthorizedError`) used across all layers. May import only `scope:shared` + `type:util`. No framework imports.
